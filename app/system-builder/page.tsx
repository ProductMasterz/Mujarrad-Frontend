import { SystemBuilder } from '@/components/system-builder/SystemBuilder';

export const metadata = { title: 'System Builder — Mujarrad' };

export default function SystemBuilderPage() {
  return <SystemBuilder />;
}
