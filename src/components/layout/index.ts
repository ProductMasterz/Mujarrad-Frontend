// Layout Components
export { AppLayout } from './AppLayout';
export { AppHeader, TabsBar, Breadcrumb, HomeIcon, type Tab } from './Header';
export { AppSidebar, SidebarItem, NodeTypeIcon, type SidebarNode } from './Sidebar';

// Keep existing Navbar for backwards compatibility
export { Navbar } from './Navbar';
